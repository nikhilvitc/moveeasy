rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for testing the platform prototype
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 6, 1);
    }
  }
}
